steps:
- name: 'gcr.io/cloud-builders/docker'
  env:
  - 'DOCKER_BUILDKIT=1'
  args: [
    'build', 
    '--build-arg', 'BUILDKIT_INLINE_CACHE=1', 
    '-t', '${_IMAGE_NAME}', 
    '.'
  ]
images:
- '${_IMAGE_NAME}'
